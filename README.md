# 排序演算法分析與實作報告

學號：11428144  
姓名：郭志楷  

模擬展示頁面：  
https://olic951004.github.io/sort_report/

---

## 一、前言

排序（Sorting）是計算機科學中最基礎且重要的問題之一，  
其目的在於將一組資料依照特定順序（例如由小到大）重新排列。

不同排序演算法在設計理念、執行效率與記憶體使用上皆有所差異，  
當資料規模增加時，演算法效能的差距也會被明顯放大。

本次作業透過實作並視覺化五種常見排序演算法：

- 氣泡排序（Bubble Sort）
- 選擇排序（Selection Sort）
- 插入排序（Insertion Sort）
- 合併排序（Merge Sort）
- 快速排序（Quick Sort）

除了說明其基本原理與複雜度分析外，  
也透過實際程式模擬不同資料規模下的排序時間，  
比較各演算法在實務應用中的效能差異。

---

## 二、排序演算法原理說明

### （一）氣泡排序法（Bubble Sort）

氣泡排序是一種概念最直觀的排序方法。  
其核心作法是反覆比較相鄰的兩個元素，  
若順序錯誤便進行交換，  
使較大的數值逐漸往陣列右端移動，如同氣泡向上浮動。

排序過程中，每一回合都會將目前尚未排序區域中的最大值放到正確位置。

**特點：**
- 實作簡單、容易理解
- 不適合大資料量排序

---

### （二）選擇排序法（Selection Sort）

選擇排序的作法是在每一輪排序中，  
從尚未排序的資料區段中找出最小值，  
再與該區段的第一個元素進行交換。

不論資料原本是否幾乎已排序完成，  
比較次數皆固定，因此時間複雜度不會改善。

**特點：**
- 交換次數少（最多 n − 1 次）
- 比較次數固定

---

### （三）插入排序法（Insertion Sort）

插入排序的概念類似於整理撲克牌。  
它會將陣列分成「已排序區域」與「未排序區域」，  
每次從未排序區取出一個元素，  
插入到左側已排序區域的正確位置。

當資料原本接近排序完成時，  
所需移動與比較次數會大幅減少。

**特點：**
- 適合小型資料或近乎排序完成的資料
- 實務中常作為其他排序法的輔助

---

### （四）合併排序法（Merge Sort）

合併排序是一種典型的「分治法（Divide and Conquer）」演算法。  
其做法是先將資料不斷拆分成更小的子陣列，  
直到子陣列只剩一個元素後，再逐步合併成有序陣列。

在合併過程中，會同時比較左右子陣列的元素大小，  
並依序放入暫存陣列中。

**特點：**
- 效能穩定
- 適合處理大量資料
- 需要額外記憶體空間

---

### （五）快速排序法（Quick Sort）

快速排序同樣使用分治法，  
會先選擇一個基準值（Pivot），  
將陣列重新排列，使小於 Pivot 的元素在左側，  
大於 Pivot 的元素在右側。

接著對左右子陣列重複進行相同操作，直到排序完成。

**特點：**
- 平均效能極佳
- Pivot 選擇不當時效能會惡化

---

## 三、複雜度分析

### （一）時間複雜度分析

| 排序方法 | 最佳情況 | 平均情況 | 最差情況 |
|--------|--------|--------|--------|
| 氣泡排序 | O(n) | O(n²) | O(n²) |
| 選擇排序 | O(n²) | O(n²) | O(n²) |
| 插入排序 | O(n) | O(n²) | O(n²) |
| 合併排序 | O(n log n) | O(n log n) | O(n log n) |
| 快速排序 | O(n log n) | O(n log n) | O(n²) |

---

### （二）空間複雜度分析

| 排序方法 | 空間複雜度 | 是否需額外記憶體 |
|--------|----------|--------------|
| 氣泡排序 | O(1) | 否 |
| 選擇排序 | O(1) | 否 |
| 插入排序 | O(1) | 否 |
| 合併排序 | O(n) | 是 |
| 快速排序 | O(log n) | 是（遞迴堆疊） |

---

## 四、實驗設計與數據模擬

### （一）實驗目的

透過實際程式執行五種排序演算法，  
比較在不同資料規模（n 值）下的執行時間，  
驗證理論時間複雜度與實際效能表現之差異。

---

### （二）實驗設定

- 使用隨機產生之整數陣列
- 每一種排序法皆使用相同資料內容進行測試
- 測試資料量：
  - n = 100
  - n = 1,000
  - n = 5,000
  - n = 10,000
- 取多次實驗平均結果以降低誤差

---

### （三）實驗實作方式（概念）

```javascript
function testSort(sortFunc, arr) {
  const data = [...arr];
  const start = performance.now();
  sortFunc(data);
  const end = performance.now();
  return end - start;
}

## 五、實驗結果比較與分析

### （一）排序時間結果比較

以下表格整理五種排序法在不同資料筆數（n）下的實驗結果，
排序時間單位為毫秒（ms），數值取多次實驗之平均值。

| 資料筆數 n | Bubble Sort | Selection Sort | Insertion Sort | Merge Sort | Quick Sort |
|-----------|-------------|----------------|----------------|------------|------------|
| 100 |  |  |  |  |  |
| 1,000 |  |  |  |  |  |
| 5,000 |  |  |  |  |  |
| 10,000 |  |  |  |  |  |

---

### （二）實驗結果觀察

根據實驗數據，可以觀察到以下幾點趨勢：

1. **在資料筆數較小（n = 100）時**  
   各排序法之間的執行時間差距不明顯，  
   顯示在小型資料集下，演算法選擇對效能影響有限。

2. **當資料筆數逐漸增加時**  
   氣泡排序、選擇排序與插入排序的執行時間明顯快速成長，  
   符合其理論時間複雜度 O(n²) 的特性。

3. **合併排序與快速排序在大資料量下表現最佳**  
   當 n 成長至 5,000 以上時，
   O(n log n) 演算法的時間優勢開始明顯展現。

4. **快速排序平均效能最快，但結果不完全穩定**  
   在某些測試中快速排序略快於合併排序，
   但其效能會受到 Pivot 選擇方式影響，
   在特定資料排列下可能退化。

---

### （三）理論與實驗結果比較

整體而言，實驗結果與理論時間複雜度分析趨勢一致：

- O(n²) 演算法不適合用於大型資料排序
- O(n log n) 演算法在實務應用中具備明顯優勢
- 插入排序在資料接近排序完成時仍具實用價值

此結果驗證了演算法設計對效能有決定性影響。

## 六、心得與結論

透過本次排序演算法的實作、視覺化展示與效能實驗，
我對不同排序法的運作流程有了比只閱讀課本更深入的理解。

在實際觀察排序動畫的過程中，
可以清楚看出各演算法在比較、交換與資料移動上的差異，
尤其對初學者而言，視覺化大幅降低了理解門檻。

在實驗結果方面，
也實際體會到時間複雜度的差異會隨資料量放大而變得非常明顯，
這讓原本抽象的 O(n²) 與 O(n log n) 概念具體反映在執行時間上。

特別是合併排序與快速排序，
讓我更熟悉遞迴與分治法的實際應用方式，
並理解為何在實務系統中，大多會選擇這類高效率排序法。

總結來說，本次作業不僅加深我對排序演算法的理論認識，
也讓我學會如何透過實驗數據驗證理論，
對日後學習資料結構與演算法奠定良好的基礎。
